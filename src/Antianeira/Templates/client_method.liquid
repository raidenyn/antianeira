{{ client_method.comment | comment }}
public {{ client_method.name }}({% if client_method.has_arguments %}request: {{ client_method | request }}{% endif %}): Observable<{{ client_method | response }}> {
	const url: string = '{{ client_method | url }}';

    const options = {
		{% if client_method.has_arguments %}
			{% if client_method.has_body %}
			body: request,
			{% endif %}
			{% if client_method.has_params %}
			params: request,
			{% endif %}
		{% endif %}
        method: '{{ client_method | method }}'
    };

    return this._httpClientUtils.ajax(url, options);
}
